# Frontend

Static frontend for uploading X-ray images to the backend inference API.

Because this is a simple HTML/CSS/JS app, you can open `index.html` directly or
serve the folder with any static file server.
